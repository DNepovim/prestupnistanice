---
import { Image } from 'astro:assets'
import { getEntry } from 'astro:content'
import { Icon } from 'astro-icon/components'
import { isDefined, isNonEmptyString } from 'narrowland'

import Tooltip from '../components/Tooltip.astro'
import { routes } from '../routes'
import { joinWords } from '../utils/joinWords'
import { rolesMap } from '../utils/rolesMap'

type Props = {
  slug: string
  imperfect?: boolean
  roles?: string[]
  role?: string
  col?: string
}

const { slug, imperfect, roles, role, col } = Astro.props

const entry = await getEntry('author', slug)

if (!entry) {
  return
}

const { data: author } = entry
---

<a
  class="group flex items-center gap-4 rounded-md px-4 py-2 transition-colors"
  href={routes.authors.detail(slug)}
>
  {
    author.image ? (
      <Image
        src={author.image}
        alt={`${author.firstname} ${author.surname}`}
        width={40}
        height={40}
        class="size-10 rounded-full"
      />
    ) : (
      <div class="from-brand-first-200 to-brand-second-200 size-10 shrink-0 rounded-full bg-gradient-to-tr" />
    )
  }
  <div>
    {
      !imperfect && (
        <div class="text-sm text-gray-500">
          {joinWords([
            ...(roles ?? [])
              .map((r) => rolesMap[r]?.perfect[author.gender])
              .filter(isDefined),
            ...(role ? [role] : []),
          ])}
        </div>
      )
    }
    <p
      class="group  relative text-alt font-alt text-brand-first-700 inline-flex items-center gap-2"
    >
      {author.firstname}
      {author.surname}
      {
        isNonEmptyString(col) && (
          <Tooltip label={col}>
            <Icon name="ph:info" class="size-4" />
          </Tooltip>
        )
      }
      <span
        class="bg-brand-first-500 absolute bottom-0 left-0 h-[1px] w-0 transition-all duration-600 group-hover:w-full"
      ></span>
    </p>
    {
      imperfect && (
        <div class="text-sm text-gray-500">
          {joinWords([
            ...(roles ?? [])
              .map((r) => rolesMap[r]?.imperfect[author.gender])
              .filter(isDefined),
            ...(role ? [role] : []),
          ])}
        </div>
      )
    }
  </div>
</a>
